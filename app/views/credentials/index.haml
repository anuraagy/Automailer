- if current_user.present?
  #dash
    .container.top-margin
      .content-box
        %h1.inline-block Your Email Credentials
        .inline-block.pull-right
          = link_to "+ New Credential", "javascript:void(0)", class: "btn btn-primary", id: "new-campaign-btn"
        %hr
        .row
    #modal-container
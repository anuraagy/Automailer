.col-md-4
  .card.campaign-card
    .card-body
      %p
        = credential.name
      = link_to "Manage", credential_path(credential), class: "badge"
      = link_to "Delete", credential_path(credential), method: :delete, class: "badge"
